from setuptools import setup

setup(
    name = "jalpy.py",
    version = "0.1",
    py_modules = ['jalpy'],
    url = "https://github.com/demotomohiro/remocolab",
    author = "celle2",
    install_requires = ["installCRD"]
)
